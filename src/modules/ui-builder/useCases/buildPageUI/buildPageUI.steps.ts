import { BuildPageUI } from "./buildPageUI";
import { stringify } from "querystring";

// describe("Given a page on a digital product, should build the UI component", () => {
//   describe(`
//   Should build a component for page with Hero, List of Places, Who We are Section 
//   `, async () => {
//     const input = {
//       digitalProductId: "dp#1",
//       pageId: "p#1",
//     };

//     const useCase = await new BuildPageUI().execute(input);

//     expect(useCase.isOk()).toBeTruthy();

//     const pageUI = useCase.unwrap();

//     //still don't know how to test this. Is it only really general because I'm gonna test everything else through domain?
//     expect(pageUI);
//   });
// });
