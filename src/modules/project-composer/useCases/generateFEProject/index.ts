import { integrationRepo } from "../../repos";
import { GenerateFEProject } from "./generateFEProject";

const generateFEProject = new GenerateFEProject(integrationRepo);

export { generateFEProject };
